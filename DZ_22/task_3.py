# 3. Написати sql запит, який виведе інформацію про вік (кількість років)
# та кількість користувачів, які відповідають цьому віку.
# Результатом виконання такого запиту має бути таблиця:

    #age  | users

    #32    | 1

    #52    | 2

    #120  | 2

    #1142 | 1


# запит:

# SELECT age, COUNT(id) AS user FROM users GROUP BY age;

# дані що в базі не відповідають необхідним результатам. 120 років тільки одному юзеру
# я залила всі отримані результати в таблиці різних форматів